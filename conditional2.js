// LOGICAL OPERATORS
// && AND (all must be true)
// ll or (either is true)
// == comparison (are the the same)
// === strict comparison (compares both value and datatype)
// != NOT Equal 
// !== strict 
// <= less than or equal to
// >= greater than or equal to
// > greater than
// < less than 

height = 60
minimum = 55

console.log("start") // always runs

if (height >= minimum){ //depends on the condition
    console.log("weee!!!");
}else{
    console.log("watching the fun")
}

