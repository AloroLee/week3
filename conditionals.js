// COMPUTER VALUES ARE 0 and 1, AKA off and on
// LOGIC TRUE OR FALSE, 0 is usually false and non-zero is usually true

// Bool - Boolean (true/false) - Reason/Philosophy into math?
// && combines two boolean

//INPUTS
hasCar = true
hasLicense = false
canReachPedals = true


const meetsAllCriteria = hasLicense
//CALCULATION
console.log(hasLicense)

//DECISION
if(hasLicense){ //acts as a gate 
    console.log("Zoom Zoom") // did not run??
}

hasLicense = true

console.log(hasLicense)

if(hasLicense){ //acts as a gate 
    console.log("Zoom Zoom") // did not run??
}